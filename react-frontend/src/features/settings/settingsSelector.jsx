export const selectCurrentPeriod = (state) => state.settings.currentPeriod;
export const selectFilterCategory = (state) => state.settings.filter;
export const selectSort = (state) => state.settings.sort;
export const selectTheme = (state) => state.settings.theme.mode;
