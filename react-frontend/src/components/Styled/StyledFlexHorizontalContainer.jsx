import { styled } from "@mui/material";

const StyledFlexHorizontalContainer = styled("div")({
    flex: 1, // grow or shrink to fit full space = flexGrow: 1

    display: "flex",
    flexDirection: "row",
});

export default StyledFlexHorizontalContainer;
