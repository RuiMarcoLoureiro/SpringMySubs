import { styled } from "@mui/material";

const StyledFlexVerticalContainer = styled("div")({
    flex: 1, // grow or shrink to fit full space = flexGrow: 1

    display: "flex",
    flexDirection: "column",
});

export default StyledFlexVerticalContainer;
